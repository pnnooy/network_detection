# 项目结题报告 —— 分工

## 分工

| 角色 | 负责人 | 任务 | DDL |
|------|--------|------|-----|
| 框架+填充 | 李哲 | 使用自己的 LaTeX 模板，填充全部内容 | 7/28 18:00 → 陈志恒 |
| 审校 | 陈志恒 | 检查内容正确性和格式问题，修正后定稿 | 7/29 23:59 → 韩宇飞 |

---

## 李哲

使用你自己的 LaTeX 模板写结题报告。

**内容参考**（按重要性排序）：
1. Canvas 结题报告模板的大纲结构（只看有哪些章节，格式用你自己的）
2. `README.md` —— 项目全貌、架构、各模块说明、开发历程
3. `docs/interface_spec.md` —— 接口规范、数据格式
4. `src/` 目录代码和 docstring —— 各模块具体实现

**要求**：
- 单栏排版
- 用 AI 批量填充初稿后，手动修正技术细节与代码一致
- 图表自己绘制，或使用项目已有的截图/架构图
- 字数无硬性要求，内容完整即可

---

## 陈志恒

拿到李哲的 `.tex` 和 `.pdf` 后做审校。

**要求**：
- 检查并修正所有格式问题（编译错误、图表编号、参考文献格式、排版错乱等）
- 检查内容正确性（关键数字如 426/27/30/157 是否正确，技术描述与代码是否一致，人名模块对应是否准确）
- 明显的错别字和语法错误修正
- 保证全文通顺、无重大错误即可，不需要逐句润色

---

## 时间线

| 时间 | 事件 |
|------|------|
| 7/28 18:00 | 李哲 → 陈志恒：`.tex` + `.pdf` + 图片 |
| 7/29 23:59 | 陈志恒 → 韩宇飞：审校后的 `.tex` + `.pdf` |
